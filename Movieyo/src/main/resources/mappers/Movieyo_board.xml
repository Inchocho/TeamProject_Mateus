<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movieyo.board">

	
	<select id="count" resultType="int">
        SELECT BOARD_COUNT(*) FROM MVY_BOARD
    </select>

    <delete id="deleteAll">
        DELETE FROM MVY_BOARD
    </delete>

	<delete id="boardDeleteOne" parameterType="int">
		DELETE FROM MVY_BOARD
		WHERE BOARD_NO = #{BOARD_NO}
	</delete>
	
    <insert id="insert" parameterType="BoardDto">
        INSERT INTO MVY_BOARD
            (USER_NO, BOARD_HEAD, BOARD_TITLE)
        VALUES
            (#{USER_NO}, #{BOARD_HEAD}, #{BOARD_TITLE})
    </insert>

    <select id="selectAll" resultType="BoardDto">
        SELECT BOARD_NO, USER_NO, BOARD_HEAD, BOARD_TITLE, BOARD_CONTENT, BOARD_CREDATE, BOARD_MODDATE,BOARD_COUNT
        FROM MVY_BOARD
        ORDER BY boardCredate DESC, boardNO DESC
    </select>

    <sql id="selectFromBoard">
        SELECT BOARD_NO, USER_NO, BOARD_HEAD, BOARD_TITLE, BOARD_CONTENT, BOARD_CREDATE, BOARD_COUNT
        FROM MVY_BOARD
    </sql>

    <select id="select" parameterType="int" resultType="BoardDto">
        <include refid="selectFromBoard"/>
        WHERE BOARD_NO = #{BOARD_NO}
    </select>

    <select id="selectPage" parameterType="map" resultType="BoardDto">
        <include refid="selectFromBoard"/>
        ORDER BY BOARD_CREDATE DESC, BOARD_NO DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <update id="update" parameterType="BoardDto">
        UPDATE MVY_BOARD
        SET   BOARD_TITLE = #{BOARD_TITLE}
          , BOARD_CONTENT = #{BOARD_CONTENT}
          , UP_DATE = NOW()
        WHERE BOARD_NO = #{BOARD_NO}
    </update>


    <update id="increaseViewCount" parameterType="int">
        UPDATE MVY_BOARD
        SET   BOARD_COUNT = BOARD_COUNT + 1
        WHERE BOARD_NO = #{BOARD_NO}
    </update>

</mapper>